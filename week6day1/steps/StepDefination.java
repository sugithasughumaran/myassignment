/*package steps;

import org.openqa.selenium.By;

import based.week6d1base;
import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;

public class StepDefination extends week6d1base{
	
	@Then("the user keyin the username {string}")
	public void the_user_keyin_the_username(String Uname) {
		driver.findElement(By.id("username")).sendKeys(Uname);
	}
	@Then("the user keyin the password {string}")
	public void the_user_keyin_the_password(String pass) {
		driver.findElement(By.id("password")).sendKeys(pass);
	}
	@Then("the user click the login button in the portal")
	public void the_user_click_the_login_button_in_the_portal() {
		driver.findElement(By.className("decorativeSubmit")).click();
	}

}*/
